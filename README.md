# Configuring-Switch-Virtual-Interfaces-on-Layer-3-Switches
A Switch Virtual Interface (SVI) is a logical Layer 3 interface on a multilayer switch that represents a VLAN and enables routing between VLANs, eliminating the need for a separate physical router. Join me as I configure SVI on Cisco Catalyst 3560.
